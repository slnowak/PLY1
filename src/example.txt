float a = 0.0, b = 10.6, c = 0.0;
int x = 5.5;
string s = "string";

int f( float x ){
	return x;
}

int gcd(int m, int n, string s) {
	
	string w = "konkate" - "nacja";

	int res = 0;
	if (s == "string"){
		return asd;
	}

	if (s == x){
		return "pp";
	}
	if (m!=n) {
		if (m > n)
			res = gcd(m-n, n);
		else
			res = gcd(n-m, m);
	}
	else
		res = m;

	print res;
	return res;
}

while(a >= b ) {
	a = 1/2*(a+b/a);
}

